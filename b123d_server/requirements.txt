flask>=3.0
flask-cors>=4.0
# Pinned to match VERSIONS.json's expected build123d (0.10.0) so the deployed
# kernel doesn't drift from the wire/naming contract. Bump both together.
build123d==0.10.0
# AI proxy (b123d_server/ai_proxy.py) — forwards /ai/chat to the Anthropic
# Messages API, streaming SSE through with stream=True.
requests>=2.31
# Used by build_sidecar.py to freeze this server into a Tauri externalBin.
# Only needed when producing the desktop sidecar; the runtime doesn't import it.
pyinstaller>=6.0
python-dotenv>=1.0
# Stripe billing (b123d_server/billing.py) — only needed when billing is
# configured; billing.py imports stripe lazily so the kernel runs without it.
stripe>=7.0
